;; user/visual/aa.el

(setq u/:init/:module/:sub/:visual
      '(
	(/visual/indent . "u-indent.el")
	(/visual/theme . "u-theme.el")
	)
      )

(u/:init/:module/:aa
 "visual"
 u/:init/:module/:sub/:visual)
