;; user/lang/c_cpp/gtags.el
;; C C++ C

; with-temp-buffer
; rtags-call-rc --dump-compile-commands

; lsp-mode + flycheck + clangd
; gtags

(use-package company-ctags
  :ensure t
  :init
  :config
                                        ;
  )

;; (use-package 
